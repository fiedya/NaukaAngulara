import { AppRoutes } from './routes';

export class AppRoutesNavigate {
    public static readonly HomePage: string = `${AppRoutes.HomePage}`;
    public static readonly SecondPage: string = `${AppRoutes.SecondPage}`;
}
